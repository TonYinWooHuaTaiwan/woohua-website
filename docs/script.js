const menu=document.querySelector('.menu');const nav=document.querySelector('nav');const lang=document.querySelector('.lang');const trans=[...document.querySelectorAll('[data-en][data-zh]')];let current='en';function apply(v){current=v==='zh'?'zh':'en';document.documentElement.lang=current==='zh'?'zh-Hant':'en';trans.forEach(el=>el.textContent=current==='zh'?el.dataset.zh:el.dataset.en);if(lang)lang.textContent=current==='zh'?'EN':'中'}menu?.addEventListener('click',()=>{const open=nav?.classList.toggle('open');menu.setAttribute('aria-expanded',String(Boolean(open)))});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu?.setAttribute('aria-expanded','false')}));lang?.addEventListener('click',()=>apply(current==='zh'?'en':'zh'));apply((navigator.language||'').toLowerCase().startsWith('zh')?'zh':'en');